# 2026-05-07 今日工作进度

- 项目：收念匣 / Shounian Box Miniapp
- 当前状态：默认版继续打磨中，今日重点为首页入口层级重构、设置弹层落地、写符弹层输入法适配，以及评语文案池升级。

## 今日目标

1. 调整首页按钮层级，让“写功 / 写过”“卡片 / 封存”“玩法 / 设置”职责更清楚。
2. 为设置入口落地第一批可用功能：清除当天、清除全部。
3. 修复卡片页和组件样式中的小程序 WXSS 限制问题。
4. 优化写符弹层在输入法弹起时的可用性。
5. 将“今日评语”从少量固定文案升级为更丰富、稳定随机的文案池。

## 已完成

- 首页按钮层级调整：
  - 底部主按钮保留“写一功 / 写一过”。
  - 底部次级按钮收敛为“卡片 / 封存”。
  - “玩法 / 设置”移到首页右上角，改为轻量辅助入口。
- 首页玩法弹层继续保留，并从底部入口改为右上角问号入口。
- 设置弹层已落地首批功能：
  - “清除当天”可清空今天的功符、过符和封存状态。
  - “清除全部”可清空今天记录和全部归档。
  - 两个动作均接入确认弹窗与执行后 toast 提示。
- 数据层补充清理能力：
  - `miniprogram/services/record-service.js` 新增 `clearToday()`。
  - `miniprogram/services/record-service.js` 新增 `clearAll()`。
- 卡片页问题修复：
  - 修复 `sealed-card` 组件和 `summary` 页中不被小程序组件 WXSS 允许的选择器。
  - `summary` 页卡片容器补居中壳层，卡片展示更稳定。
- 写符弹层体验优化：
  - 弹层整体高度和输入区长度收短。
  - 输入法弹起时，符纸弹层会根据键盘高度整体上移；收起后恢复原位。
  - 修正键盘高度从 px 到 rpx 的换算，避免按钮只露出一点点的问题。
- 设置弹层视觉继续打磨：
  - 清理按钮补上明确控件样式。
  - 弹窗整体上移并拉长，按钮组与“知道了”之间间距增大。
- 今日评语升级：
  - 说明类文案和展示类文案继续集中在 `miniprogram/config/default-copy.js`。
  - `empty / gong / balanced / guo` 四组评语改为 `titlePool + copyPool`。
  - 评语选择逻辑改为按“日期 + 状态 + 功过数量”稳定选择，同一天刷新不乱跳，记录变化时可自然更新。
  - 第二轮重点扩充并重写了 `balanced` 与 `guo` 组，降低模板化重复感。
- Midjourney / 主题替换方向已记录暂停结论：
  - 偏写实单张图与当前图层、光源、动画锚点不够贴合。
  - 后续若重启，优先考虑二维插画和分层透明 PNG。
- 主题规则和资源接入规范文档已补充：
  - 主题目录规则、文件命名和 fallback 规则已写入文档。
  - 通用主题注册与默认 fallback 方向已在代码中预留。

## 今日验证

已完成的本地验证包括：

- `miniprogram/**/*.js` 级别 `node --check` 静态语法检查。
- 对 `today / summary / action-bar / paper-slip / sealed-card / record-service` 等关键文件的定向检查。
- 扫描组件 WXSS 中 `:host`、标签选择器、属性选择器等已知不兼容模式，并逐步清理已发现的问题。
- 对评语池结构和稳定选择逻辑做字段链路检查，确认 `today / summary / archive` 仍统一读取 `{ title, copy }`。

## 当前观察与结论

- 心灯规则维持原方案：继续按功过差值阶梯变化，不按总符数改动。
- 评语短标题仍允许少量重复，这是有意保留的稳定感；正文已比第一版更有层次。
- 说明类文案与展示类文案已基本集中到文案配置中，但不追求把 `appName`、页面导航标题等技术配置也完全抽离。

## 风险与未完成项

- 顶部辅助入口、设置弹层和按钮布局仍需以微信开发者工具和真机最终表现为准。
- `清除 7 天前` 还未实现，目前仅完成 `清除当天 / 清除全部`。
- 组件样式文件仍存在 Windows 行尾转换提示，需要后续统一换行风格时一并处理。
- 主题与资源替换相关代码和占位资源仍在工作区中，后续若决定继续推进或回退，需要统一整理。
- `miniprogram/project.config.json` 仍是未跟踪文件，除非明确需要，不纳入本次改动。

## 下一步建议

1. 在真机和微信开发者工具中继续验证：
   - 右上角“玩法 / 设置”入口的视觉轻重是否合适。
   - 设置弹层按钮间距、上移位置和点击手感是否稳定。
   - 写符弹层在不同机型上随输入法上移的幅度是否合适。
2. 若设置弹层第一批功能稳定，下一步可补“清除 7 天前”。
3. 若评语方向基本满意，可继续做第三轮扩写，但建议按具体状态组定向打磨，而不是平均扩量。
4. 视需求决定是否整理主题相关未提交改动，避免与当前默认版体验线互相干扰。
